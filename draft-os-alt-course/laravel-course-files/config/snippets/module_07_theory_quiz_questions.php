<?php

/**
 * Модуль 7 · тест по теории: контроль целостности, Osec и rpm -V.
 * 15 вопросов: 13 одиночных + 2 множественных (индексы вариантов 0=А … для 6 вариантов Е=5).
 *
 * Подключение: 'theory_quiz' => require __DIR__.'/snippets/module_07_theory_quiz_questions.php',
 */
return [
    [
        'q' => 'Команда `rpm -V sudo` вернула пустой вывод. Что это означает?',
        'a' => [
            'Пакет sudo не установлен в системе',
            'Все файлы пакета sudo совпадают с эталоном в RPM-базе — изменений нет',
            'Команда завершилась с ошибкой — нужно запустить с правами root',
            'База RPM повреждена и не может выполнить проверку',
        ],
        'c' => 1, // Б: пустой вывод — нет отличий от эталона
    ],
    [
        'q' => 'Какую базу данных использует `rpm -V` для сравнения текущего состояния файлов?',
        'a' => [
            '`/etc/rpm/baseline` — специальный файл эталонов',
            '`/var/lib/rpm` — локальная база RPM, установленная вместе с системой',
            'Репозиторий ftp.altlinux.org — скачивает эталоны при каждой проверке',
            '`/var/lib/osec` — общая база для всех инструментов контроля целостности',
        ],
        'c' => 1, // Б
    ],
    [
        'q' => 'Администратор запустил `rpm -Va` и увидел: `.M.......  /usr/bin/chsh`. Что именно изменилось в файле `/usr/bin/chsh`?',
        'a' => [
            'MD5-хэш содержимого файла',
            'Владелец файла',
            'Права доступа (режим доступа)',
            'Время последней модификации',
        ],
        'c' => 2, // В: M — Mode
    ],
    [
        'q' => 'Администратор обнаружил изменённый файл через `rpm -Va`. Какая команда восстановит файлы пакета в исходное состояние?',
        'a' => [
            '`rpm -i <пакет>` — переустановить пакет',
            '`rpm --restore <пакет>` — восстановить файлы из RPM-базы',
            '`rpm -e <пакет> && rpm -i <пакет>` — удалить и установить заново',
            '`apt-get reinstall <пакет>` — единственный способ восстановления',
        ],
        'c' => 1, // Б; на части систем вместо `--restore` используют переустановку пакета
    ],
    [
        'q' => 'Почему `rpm -Va` не может обнаружить вредоносный файл `/etc/cron.d/backdoor`, созданный злоумышленником?',
        'a' => [
            '`rpm -Va` проверяет только файлы в `/bin` и `/sbin`, конфиги не контролируются',
            'Файлы в `/etc/cron.d/` защищены от проверки флагом immutable',
            'RPM знает только о файлах, установленных из пакетов — новый файл, созданный вручную, в базе отсутствует',
            '`rpm -Va` проверяет только исполняемые файлы, не текстовые',
        ],
        'c' => 2, // В
    ],
    [
        'q' => 'Osec создаёт эталонную базу при первом запуске. Где она хранится?',
        'a' => [
            '`/etc/osec/baseline.db`',
            '`/var/lib/osec/`',
            '`/var/log/osec/`',
            '`/tmp/osec/`',
        ],
        'c' => 1, // Б
    ],
    [
        'q' => 'Osec запущен на сервере. Отчёт показал: `new: /usr/sbin/backdoor`. Администратор посмотрел — файл создан вчера ночью в 3:00, никаких плановых работ не было. Какое правильное действие?',
        'a' => [
            'Запустить osec ещё раз — возможно, это ложное срабатывание',
            'Обновить базу Osec, чтобы зафиксировать новое состояние',
            'Расследовать инцидент — изучить, кто создал файл, что он делает, проверить логи входа',
            'Удалить файл и перезапустить Osec',
        ],
        'c' => 2, // В
    ],
    [
        'q' => 'Администратор установил Osec и сразу создал эталонную базу. Через неделю после ввода сервера в эксплуатацию он обнаружил, что Osec молчит, хотя конфиги явно менялись. В чём проблема?',
        'a' => [
            'Osec не настроен на проверку каталога `/etc` — нужно добавить его в `DIRS`',
            'База создана правильно, но таймер `osec.timer` не включён — проверки не запускаются',
            'Osec не умеет отслеживать изменения в конфиг-файлах, только в бинарниках',
            'После создания базы нужно перезагрузить сервер',
        ],
        'c' => 1, // Б
    ],
    [
        'multi' => true,
        'q' => 'После планового `apt-get dist-upgrade` Osec показал 500+ изменённых файлов. Выбери **все** правильные действия.',
        'a' => [
            'Убедиться, что все изменения относятся к обновлённым пакетам — сверить с `rpm -Va`',
            'Расценить это как инцидент и изолировать сервер',
            'Проверить, нет ли среди изменений файлов, которые не входят ни в один пакет',
            'Пересоздать эталонную базу Osec, чтобы зафиксировать новое легитимное состояние',
            'Отключить Osec — он мешает нормальной работе системы',
        ],
        'c' => [0, 2, 3], // А, В, Г
    ],
    [
        'q' => 'Зачем файл `/etc/resolv.conf` нужно добавить в раздел `EXCLUDE` конфига Osec?',
        'a' => [
            'Это системный файл, и RPM уже его контролирует — дублирование не нужно',
            'Файл обновляется автоматически при активации сети (в т.ч. через resolvconf) — иначе будут постоянные ложные срабатывания',
            'Osec не умеет проверять файлы в каталоге `/etc`',
            'Файл зашифрован, и Osec не может вычислить его хэш',
        ],
        'c' => 1, // Б
    ],
    [
        'q' => 'Чем управление Osec в Альт **принципиально** отличается от аналогичных инструментов в Ред ОС и «Астра»?',
        'a' => [
            'В Альт Osec работает быстрее благодаря оптимизированному алгоритму хэширования',
            'Только в Альт есть интеграция с графическим интерфейсом управления — модуль в ЦУС',
            'В Альт Osec проверяет больше файлов, чем аналоги в других дистрибутивах',
            'В Ред ОС и «Астра» контроль целостности запрещён регуляторами',
        ],
        'c' => 1, // Б
    ],
    [
        'q' => 'Администратор хочет **немедленно** проверить целостность системы вне расписания. Какие два способа это сделать в Альт? (Один верный вариант из списка.)',
        'a' => [
            '`osec --force` в командной строке или кнопка «Запустить» в ЦУС',
            '`systemctl restart osec.timer` в командной строке или через ЦУС',
            'Команда `osec` в командной строке или кнопка «Запустить» в ЦУС',
            '`rpm -Va` в командной строке или через ЦУС',
        ],
        'c' => 2, // В
    ],
    [
        'multi' => true,
        'q' => 'Какие из перечисленных файлов и каталогов **обязательно** должны быть под контролем Osec на production-сервере?',
        'a' => [
            '`/etc/passwd` — база учётных записей',
            '`/var/log/messages` — системный лог',
            '`/etc/sudoers` — права sudo',
            '`/etc/cron.d/` — задачи планировщика',
            '`/tmp/` — временные файлы',
            '`/usr/bin/sudo` — бинарник sudo',
        ],
        'c' => [0, 2, 3, 5], // А, В, Г, Е
    ],
    [
        'q' => 'Администратор вручную отредактировал `/etc/openssh/sshd_config` — добавил новый параметр. Osec при следующей проверке покажет файл как `changed`. Что нужно сделать, чтобы это не считалось инцидентом при последующих проверках?',
        'a' => [
            'Добавить `sshd_config` в `EXCLUDE` — исключить из проверки навсегда',
            'Пересоздать базу Osec — запустить `osec` заново, чтобы зафиксировать новое легитимное состояние',
            'Ничего не делать — Osec сам поймёт, что изменение легитимное',
            'Откатить изменения в `sshd_config` — нельзя менять файлы под контролем Osec',
        ],
        'c' => 1, // Б
    ],
    [
        'q' => 'Верно ли утверждение: «Если `rpm -Va` не показывает изменений — система не скомпрометирована»?',
        'a' => [
            'Верно — RPM проверяет все файлы системы',
            'Верно — RPM имеет доступ к эталонам всех файлов, включая конфиги',
            'Неверно — RPM проверяет только файлы из пакетов; файлы, созданные вручную, новые скрипты в `/etc/cron.d/`, изменения в `/etc/sudoers.d/` RPM не увидит',
            'Неверно — RPM проверяет файлы, но его база данных может быть скомпрометирована вместе с системой',
        ],
        'c' => 2, // В: главная мысль — неполный охват; Г тоже правдоподобно, но в курсе акцент на «файлы вне пакетов»
    ],
];
