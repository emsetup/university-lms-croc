<?php

/**
 * Модуль 8 · тест по теории: «Аудит событий. auditd в Альт Линукс».
 * 15 вопросов: 13 одиночных (c — int) + 2 множественных (c — int[], чекбоксы; индексы 0=А, 1=Б, 2=В, 3=Г, 4=Д).
 *
 * Подключение: 'theory_quiz' => require __DIR__.'/snippets/module_08_theory_quiz_questions.php',
 */
return [
    [
        'q' => 'Чем принципиально отличается **auditd** от **journald** с точки зрения уровня мониторинга?',
        'a' => [
            '**auditd** работает на уровне ядра и перехватывает системные вызовы; **journald** собирает логи приложений и служб в пользовательском пространстве',
            '**auditd** быстрее **journald**, потому что пишет в бинарный формат',
            '**journald** работает на уровне ядра, **auditd** — на уровне приложений',
            'Отличий нет: оба инструмента собирают одинаковые события разными методами',
        ],
        'c' => 0,
    ],
    [
        'q' => 'Администратор добавил правило: `-w /etc/passwd -p wa -k user_accounts`. Что означают флаги **`-p wa`**?',
        'a' => [
            'Следить за чтением и выполнением файла',
            'Следить за записью в файл и изменением его атрибутов',
            'Следить за всеми операциями: чтение, запись, выполнение, атрибуты',
            'Следить только за успешными операциями записи',
        ],
        'c' => 1,
    ],
    [
        'q' => 'Зачем в правилах системных вызовов указывается параметр **`-F arch=b64`**?',
        'a' => [
            'Ограничить мониторинг только 64-битными процессами для экономии ресурсов',
            'Указать архитектуру, чтобы корректно перехватывать системные вызовы — без этого на 64-битных системах часть вызовов может не перехватываться',
            'Включить «расширенный» режим аудита, доступный только на 64-битных системах',
            'Задать формат записей в лог-файле',
        ],
        'c' => 1,
    ],
    [
        'q' => 'Что произойдёт, если применить параметр **`-e 2`** в конфигурации **auditd**?',
        'a' => [
            '**auditd** перейдёт в расширенный режим с повышенной детализацией записей',
            'Правила аудита будут заблокированы от изменения до следующей перезагрузки системы',
            '**auditd** начнёт записывать события в зашифрованный файл',
            'Все текущие правила будут удалены, аудит сбросится в начальное состояние',
        ],
        'c' => 1,
    ],
    [
        'q' => 'Какая команда покажет **все активные правила** **auditd** в реальном времени?',
        'a' => [
            '`cat /etc/audit/rules.d/audit.rules`',
            '`auditctl -l`',
            '`ausearch --rules`',
            '`systemctl status auditd --rules`',
        ],
        'c' => 1,
    ],
    [
        'q' => 'Администратор хочет найти все события с меткой **`shadow_read`** за сегодня. Какая команда **правильная**?',
        'a' => [
            '`aureport -k shadow_read --start today`',
            '`ausearch -k shadow_read --start today`',
            '`grep shadow_read /var/log/audit/audit.log`',
            '`journalctl -k shadow_read --since today`',
        ],
        'c' => 1,
    ],
    [
        'q' => 'В записи **audit.log** есть поля **`uid=0`** и **`auid=1001`**. Что означает **`auid=1001`**?',
        'a' => [
            'Идентификатор аудита процесса — технический параметр без практического значения',
            'Реальный пользователь, который инициировал сессию (**login uid**); не меняется при типовом использовании **sudo** / **su**',
            'Идентификатор группы, к которой принадлежит пользователь',
            '**UID** процесса, который сделал системный вызов',
        ],
        'c' => 1,
    ],
    [
        'q' => 'Чем отличается **`ausearch`** от **`aureport`**?',
        'a' => [
            '**ausearch** работает с текущими правилами, **aureport** — с «историческими» данными',
            '**ausearch** ищет конкретные события по критериям; **aureport** строит сводные статистические отчёты',
            '**ausearch** читает `/var/log/audit/audit.log`, **aureport** читает **journald**',
            'Это разные названия одной утилиты с одинаковым функционалом',
        ],
        'c' => 1,
    ],
    [
        'q' => 'Почему файлы правил в **`/etc/audit/rules.d/`** рекомендуется хранить с правами **`600`** и владельцем **root**?',
        'a' => [
            'Это требование ФС: **auditd** не запустится без этих прав',
            'Если злоумышленник узнаёт, какие события логируются, он сможет действовать так, чтобы не попасть в журнал',
            'Права **600** нужны для корректной ротации лог-файлов',
            'Это требование только для систем с сертификацией ФСТЭК',
        ],
        'c' => 1,
    ],
    [
        'q' => 'Какие из перечисленных событий **auditd** может фиксировать **без дополнительных правил** (в типовой поставке с включённым аудитом аутентификации и демоном)? Отметьте **все** верные.',
        'a' => [
            'Успешные и неудачные попытки входа в систему (события аутентификации)',
            'Все команды, выполненные через **sudo**',
            'Запуск и остановка самого демона **auditd**',
            'Изменение файла `/etc/passwd`',
            'Изменение правил **auditd** через **auditctl** (конфигурация подсистемы аудита)',
        ],
        'c' => [0, 2, 4],
    ],
    [
        'q' => 'Администратор хочет **временно** протестировать правило перед добавлением в постоянный файл. Какой способ **правильный**?',
        'a' => [
            'Добавить в `/etc/audit/rules.d/test.rules` и перезапустить **auditd**',
            'Добавить через **`auditctl`** — правило применится немедленно, но не переживёт перезагрузку без записи в файл',
            'Отредактировать `/var/log/audit/audit.log` для проверки формата',
            'Использовать `systemctl reload auditd` с флагом `--test`',
        ],
        'c' => 1,
    ],
    [
        'q' => 'В **Астра Линукс** правила **auditd** содержат поля **`subj_type=psaud`** и **`obj_type=faud`**. Что будет, если применить эти правила на сервере **Альт Линукс**?',
        'a' => [
            'Правила применятся корректно — **auditd** одинаковый на всех дистрибутивах',
            'Правила будут применены, но поля **Parsec** будут игнорироваться',
            '**auditctl** отклонит правила с неизвестными полями — они специфичны для **Parsec**, которого в стандартном **Альт** нет',
            'Правила применятся, если установить пакет **parsec-compat** из репозитория **Альт**',
        ],
        'c' => 2,
    ],
    [
        'q' => 'Параметр **`disk_full_action = HALT`** в `/etc/audit/auditd.conf` означает:',
        'a' => [
            'При заполнении диска старые лог-файлы удаляются автоматически',
            'При заполнении диска **auditd** останавливается, но система продолжает работать',
            'При заполнении диска система **полностью** останавливается — работа без аудита считается недопустимой',
            'При заполнении диска **auditd** переключается на запись в `/tmp`',
        ],
        'c' => 2,
    ],
    [
        'q' => 'Выберите **все** правила, которые имеет смысл включить в **базовый** набор для сервера **Альт** с учётом его специфики.',
        'a' => [
            '`-w /etc/net/ifaces/ -p wa -k network_config` — **etcnet**, характерно для **Альт**',
            '`-w /etc/sysconfig/network/ -p wa -k network_config` — путь, типичный для **Ред ОС**',
            '`-w /etc/passwdqc.conf -p wa -k pam_config` — политика паролей (**pam_passwdqc**), характерно для **Альт**',
            '`-w /etc/tcb/ -p wa -k tcb_change` — хранилище паролей **TCB**, характерно для **Альт**',
            '`-w /etc/pam.d/ -p wa -k pam_config` — конфигурация **PAM** (универсально для Linux, но нужно в базовом наборе)',
        ],
        'c' => [0, 2, 3, 4],
    ],
    [
        'q' => 'Администратор запустил **`aureport --start today`** и увидел много событий **`USER_AUTH`** с результатом **fail** с **одного IP-адреса**. Что это **скорее всего** означает?',
        'a' => [
            'Это нормальное поведение: пользователи часто ошибаются при вводе пароля',
            'Признак **брутфорса** по **SSH** — изучить детали через **`ausearch`** (фильтр по типу события и **`--success no`**) и рассмотреть блокировку IP (**firewall** / политика доступа)',
            'Ошибка конфигурации **auditd**: дублирование записей',
            'Служба **auditd** перегружена и пишет ошибочные данные',
        ],
        'c' => 1,
    ],
];
